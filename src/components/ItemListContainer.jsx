import React from 'react';

const ItemListContainer = ({information }) => {
  return (
    <div className="item-list-container">
      <h1>{information}</h1>
    </div>
  );
};

export default ItemListContainer;